import math
#ou from import math pow
#calcular trigonometria
cat1 = float(input('Digite o primeiro  valor ?'))
cat2 = float(input('Digite o segundo valor ?'))

#calculo
resultado = math.hypot(cat1,cat2)


#resultados junto com o print
print('Seu resultado é ? {} '.format(pow(resultado)))
