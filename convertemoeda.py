#conversor de moeda com valor atual 

real = float(input('Digite a quantidade de diheiro voce possui? '))
dolls = float(input('digite o valor do dolar atual '))

#conversão
#resultado = real * dolls 

#mostrar resultado
#print('R$ {} convertido atualmente você terá U$D {:.2f} '.format(real, resultado))

#segunda formula 
print('R$ {} convertido atualmente você terá U$D {:.2f} '.format((real), (real*dolls)))
